<!doctype html>
<html lang="en">
<head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render "layouts/googleanalytics" %>
    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= javascript_include_tag "application", :async => true %>

    <%= csrf_meta_tags %>

    <%= render "layouts/shim" %>
    <%= render "layouts/googletag" %>
    <%= render "layouts/bingconversions" %>
    <%= render "layouts/facebookpixel" %>
    <%= render "layouts/favicon" %>
    <%= render "layouts/pinterest" %>
    <%= render "layouts/snapchat" %>
    <%= render "layouts/stamped" %>
    <%= render "layouts/paypalinsights" %>
    <!-- Pinterest Domain Verification -->
    <meta name="p:domain_verify" content="fbd4f0ba8a75ae5ad8a92473521f80dd"/>
    <%= tag(:link, rel: 'manifest', href: asset_path('pwa_manifest.json')) %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= full_description(yield(:description)) %>">
    <%= canonical_tag -%>
    <!--<meta name="description" content="NeuEve Suppositories and Cream are a natural remedy for vaginal dryness, atrophy, odor, itching, burning, painful sex and other discomforts of menopause. NeuEve is hormone-free and estrogen-free, and works by restoring and re-balancing your vaginal ecology.">-->

</head>
<body>
  <%= render "layouts/header" %>
  <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
  <%= yield %>
  <%= render "layouts/order_now" if (!yield(:title).include?('rder') &&
          !yield(:title).include?('Bacterial Vaginosis') &&
          !yield(:title).include?('Vaginal Odor') &&
          !yield(:title).include?('BV') &&
          !yield(:title).include?('Contact') &&
          !yield(:title).include?('About') &&
          !yield(:title).include?('Renjie Chang') &&
          !yield(:title).include?('Kevin Tao') &&
          !yield(:title).include?('Sea Buckthorn')
  ) %>

    <p class="text-center">
        Last modified: <%= yield(:last_modified) %>
    </p>
  <%= render "layouts/footer" %>

  <%= render "layouts/reamaze_chat" %>
<%= render "layouts/remarketingtag" %>
<%= render "layouts/refersion" %>
<%= render "layouts/amz_associates_one_link" %>
<%= render "layouts/linkedin" %>
<div class="d-none">
  <%= show_svg('orion-svg-sprite.svg') %>
</div>
</body>

<%= render "layouts/googleanalyticspageview" %>
</html>
